\usepackage{eso-pic}
\usepackage{graphicx}

% Custom cover page with full-page background image
\newcommand{\coverpage}{%
  \begin{titlepage}
    \AddToShipoutPictureBG*{%
      \AtPageLowerLeft{%
        \includegraphics[width=\paperwidth,height=\paperheight]{illustrations/cover-design.png}%
      }%
    }%
    \vfill
    % Title and metadata can be overlaid here if needed
    % For now, the image serves as the complete cover
  \end{titlepage}
  \clearpage
}
